<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sweng.fiktion">

    <!-- Permissions -->


    <!-- Google Maps -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <permission android:name="ch.epfl.sweng.fiktion.permission.MAPS_RECEIVE" />

    <uses-permission android:name="ch.epfl.sweng.fiktion.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Google Maps OpenGL -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.LocationActivity"
            android:label="@string/title_activity_location">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ch.epfl.sweng.fiktion.views.MainActivity" />
        </activity>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCq0bRqkZxZBbjlLkz_0Us7LNxFWBGPMGk" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".views.SimpleAddPOIActivity" />
        <activity android:name=".views.FindNearestPoisActivity" />
        <activity android:name=".views.DisplayNearestPoisActivity" />
        <activity android:name=".views.SignInActivity" />
        <activity android:name=".views.UserDetailsActivity" />
        <activity android:name=".views.RegisterActivity" />
        <activity android:name=".views.AddPOIActivity" />
        <activity android:name=".views.GetLocationFromMapActivity"></activity>
    </application>

</manifest>